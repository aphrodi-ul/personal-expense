# Copy to .env (optional). The app runs with sensible defaults if these are unset.

# Port the server listens on
PORT=3000

# Secret used to sign JWT auth tokens. CHANGE THIS in production.
JWT_SECRET=change-me-to-a-long-random-string

# Path to the SQLite database file (defaults to ./data.db)
DB_PATH=./data.db
